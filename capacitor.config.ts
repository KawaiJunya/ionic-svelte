import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'svelte.ionic',
  appName: 'ionic-svelte',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
